测试 GPU 上矩阵拷贝和矩阵转置的访存性能差异
建一个 N x N 的矩阵 A，把它拷到 GPU，然后分别测试 4 种 kernel：

1. copy
    普通矩阵拷贝：B = A
2. transpose1
    真正的转置：B(nx, ny) = A(ny, nx)
    这个版本的特点是：读连续，写不连续
3. transpose2
    从代码形式看像“另一种转置”，但实际上它只是把 A 的转置内容按顺序写进 B
    它的特点是：读不连续，写连续
4. transpose3
    和 transpose2 做的事情一样，但读取时用了 __ldg()
    即：读不连续 + 只读缓存加载

程序测量每种方式的耗时，最后如果 N <= 10，还会把矩阵打印出来检查结果。

